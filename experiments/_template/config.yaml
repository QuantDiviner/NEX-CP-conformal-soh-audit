# 实验配置模板
# 复制 experiments/_template 为新实验目录后填写。
# 注意：本仓库的实验脚本（scripts/run_*.py）均为自包含脚本，
# 通过命令行参数读取配置，不读取本文件；本文件仅作记录用途。

# 实验元信息
experiment:
  name: "expXXX_description"
  description: "实验目的描述"
  date: "YYYY-MM-DD"

# === 实验参数记录 ===
# 示例：
# model:
#   hidden_dim: 128
#
# training:
#   learning_rate: 0.0001
