# ============================================================================
# Python dependencies — NEX-CP-conformal-soh-audit
#
#   pip install -r requirements.txt
#
# The four core pins (numpy/pandas/scikit-learn/scipy) match pyproject.toml
# and environment.yml exactly, and are the versions recorded in the experiment
# run manifests (CPython 3.11.15). Supporting-package pins satisfy the ranges
# in pyproject.toml. matplotlib is additionally required to render the
# manuscript figures (paper/source/*.qmd).
# ============================================================================

# --- Core analysis pipeline (scripts/preprocess_real_battery.py, scripts/run_*.py)
numpy==2.2.6
pandas==2.3.3
scikit-learn==1.7.2
scipy==1.15.3
matplotlib==3.11.1
pyyaml==6.0.3
openpyxl==3.1.5    # pandas engine for the CALCE .xlsx workbooks
ngboost==0.5.11    # optional NGBoost baseline in the repair runners

# --- Paper rendering only (quarto render paper/source/index.qmd)
# The manuscript executes Python inline via Jupyter; these are NOT needed
# for the analysis pipeline above. Quarto itself must be installed separately
# (https://quarto.org), together with a TeX Live installation.
# jupyter
# nbclient
# ipython
